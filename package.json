{
  "name": "academic-wellness-strategy-planner",
  "version": "1.0.0",
  "description": "Fullstack deployment configuration for Render",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix server install && npm --prefix client run build",
    "start": "npm --prefix server start"
  }
}
